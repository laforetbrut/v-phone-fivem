# Claude Code
.claude/
CLAUDE.md

# Test procedure artefacts (local QA only - never push)
test-procedures/
TEST_PROCEDURE_*.html

# Generated phone preview (rebuilt from source on demand)
preview/

# OS and editor noise
Thumbs.db
.DS_Store
*.swp
.vscode/
.idea/
# Python bytecode from tools/
__pycache__/
*.pyc

# Local test server (FXServer + frameworks) - never commit
server-test/

# Diagnostic screenshots from tools/make-shots.js. A shot marked `scratch: true` exists to be
# looked at once while chasing something, or to carry an assertion that never photographs
# anything at all. Eight of them shipped inside release 1.6.0 before this rule existed.
docs/images/zz-*.png
